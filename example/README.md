# MultiTagPicker Example

## Getting Started

To run the example, change to the `example` directory, create the files that Flutter needs for building applications, and then start Flutter.

```shell
flutter create .
flutter run
```
